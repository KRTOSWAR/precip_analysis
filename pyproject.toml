[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "downscaling"
version = "1.0.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["downscaling*"]